#include<stdio.h>
#include<string.h>


struct sit
{
	char cos;
	int yn;
}si[30];

int main()
{
	int i,n,num,f;
    int n_now,away_cos; 
	char now;//¹Ë¿Í 
	while(scanf("%d",&n),n);
	{
		away_cos=n_num=num=0;
		memset(sit,0,sizeof(sit));
		while((now=getchar())!='\n')
		{
			f=0;
			num++;
		    for(i=0;i<num;i++)
		    {
		    	if(!strcmp(now,si[i].cos))
		    	{
		    		 f=1;break;
				}
			}
			if(f)
			{
				
			}
			
			else
			{
				if(n_now<n)
				 {
				 	n_n++;
				 	si[i].yn=1;
				 }
				else
				 away_cos++; 
			}
			
		}
		
		
		
	}
}
