#include<bits/stdc++.h>

using namespace std;

const long long M = 1000000007;
long long n,l;
long long dp[100100];

int main()
{
	
	while(~scanf("%lld%lld",&n,&l))
	{
		memset(dp,0,sizeof(dp));
		
		long long ans = (long long)0;
		
		
		for(int i = 1;i <= i;i++)
		{
			for(int j = )
		}
		
		for(int i = 1;i <= n;i++)
		{
			printf("    %d %lld\n",i,dp[i]);
		}
	}
	
 return 0;
}


