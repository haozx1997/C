#include<stdio.h>
#include<string.h>



int main()
{
	int n;
	while(scanf("%d",&n),n)
	{
		int a,b,i,k[205];
		scanf("%d%d",&a,&b);
		for(i=0;i<k;i++)
		 scanf("%d",&k[i]);
		dfs(a);
		printf("%d",num); 
	}
}
